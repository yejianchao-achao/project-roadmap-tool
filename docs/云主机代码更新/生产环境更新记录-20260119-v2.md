# 生产环境更新记录 2026-01-19 (v2)

**更新时间**: 2026-01-19 16:30
**执行人**: AI Assistant
**环境**: 生产环境 (10.0.20.178)

## 更新内容

### 新特性：人员管理可见性控制
-   **后端**:
    -   `Owner` 模型增加 `visible` 字段。
    -   新增 `PUT /api/owners/<id>` 接口支持更新人员信息。
    -   更新 `get_all_owners` 和 `get_owners` 逻辑。
-   **前端**:
    -   人员管理列表增加“显示/隐藏”开关。
    -   创建/编辑项目时，负责人下拉列表过滤隐藏人员。
    -   看板视图和左侧图例自动过滤隐藏人员及其关联项目。

### 问题修复
-   修复了 **人员关联项目数量显示为0** 的问题（后端 `owner_service.py` 逻辑错误）。
-   修复了 Git 提交时的代理连接拒绝问题（本地配置）。

## 部署执行情况

-   **备份**: 成功创建数据备份 (`/root/project-roadmap/backup/data-20260119-162917.tgz`)。
-   **代码同步**: 全量同步 `backend/` 和 `frontend/` (源代码及构建产物)。
-   **服务状态**: 
    -   前端构建成功。
    -   后端服务已重启。
    -   健康检查通过 (HTTP 200)。

## 验证项
- [x] 人员管理界面开关功能正常。
- [x] 隐藏人员后，看板及项目选项正确过滤。
- [x] 人员管理列表中“关联项目数”显示正确。

# 生产环境部署 - 实际配置信息

## 📋 服务器环境信息（已确认）

### 基础信息
- **服务器IP**: 10.0.20.178
- **操作系统**: CentOS Linux 7 (Core)
- **Python版本**: 3.11.9 ✅
- **pip版本**: 24.0 ✅
- **Nginx状态**: 未安装 ❌
- **当前用户**: root
- **Home目录**: /root

### 系统资源
- **磁盘空间**: 47G (已用5.9G, 可用42G) ✅
- **内存**: 1.8G (可用1.2G) ✅
- **Swap**: 2.0G ✅

### 端口状态
- **80端口**: 未占用 ✅
- **5000端口**: 未占用 ✅
- **5173端口**: 未占用 ✅

### SSH连接信息
- **私钥路径**: /Users/a1/.ssh/id_rsa
- **用户名**: root
- **连接测试**: 成功 ✅

---

## 📦 需要上传的文件清单

### 必须上传的文件和目录

#### 1. 后端文件（backend/）
```
backend/
├── app.py                    # Flask应用入口 ✅
├── requirements.txt          # Python依赖清单 ✅
├── models/                   # 数据模型 ✅
│   ├── __init__.py
│   ├── owner.py             # 负责人模型
│   ├── productline.py       # 产品线模型
│   ├── project.py           # 项目模型
│   └── settings.py          # 设置模型
├── routes/                   # API路由 ✅
│   ├── __init__.py
│   ├── owners.py            # 负责人路由
│   ├── productlines.py      # 产品线路由
│   ├── projects.py          # 项目路由
│   └── settings.py          # 设置路由
├── services/                 # 业务逻辑层 ✅
│   ├── __init__.py
│   ├── owner_service.py     # 负责人服务
│   ├── productline_service.py  # 产品线服务
│   ├── project_service.py   # 项目服务
│   └── settings_service.py  # 设置服务
└── utils/                    # 工具函数 ✅
    ├── __init__.py
    ├── decorators.py        # 装饰器
    ├── file_handler.py      # 文件处理
    └── migrate_owners.py    # 数据迁移
```

#### 2. 前端构建产物（frontend/dist/）
```
frontend/dist/               # npm run build 生成 ✅
├── index.html              # 入口HTML
└── assets/                 # 静态资源
    ├── index-[hash].js    # 打包后的JS
    ├── index-[hash].css   # 打包后的CSS
    └── ...                # 其他资源文件
```

#### 3. 数据目录（data/）
```
data/                        # 数据存储目录 ✅
├── projects.json           # 项目数据（包含现有项目）
├── productlines.json       # 产品线数据（包含现有产品线）
├── owners.json             # 负责人数据（包含现有负责人）
└── settings.json           # 设置数据（包含现有设置）
```
**注意**: 上传本地 data/ 目录中已初始化好的数据文件，保留现有项目信息。

#### 4. 配置文件
```
.gitignore                  # Git忽略配置 ✅
README.md                   # 项目说明文档 ✅
```

### 不需要上传的文件

```
❌ node_modules/            # npm依赖包（服务器端不需要）
❌ frontend/src/            # 前端源码（已构建为dist）
❌ frontend/package*.json   # 前端依赖配置（已构建）
❌ frontend/vite.config.js  # Vite配置（已构建）
❌ .git/                    # Git仓库
❌ docs/                    # 开发文档
❌ data_backup/             # 备份数据
❌ 启动项目.command         # macOS启动脚本
❌ start.py                 # 开发启动脚本
❌ __pycache__/             # Python缓存
❌ *.pyc                    # Python编译文件
❌ venv/                    # 本地虚拟环境
```

---

## 🔧 服务器需要提前安装的依赖

### 系统级依赖（必须）

#### 1. Nginx（Web服务器）
```bash
# CentOS 7 安装命令
yum install -y epel-release
yum install -y nginx
```
**作用**: 
- 提供静态文件服务（前端页面）
- 反向代理Flask后端API
- 提供缓存和性能优化

#### 2. Python 3.8+（已安装 ✅）
```bash
# 检查版本
python3 --version
# 输出: Python 3.11.9 ✅
```
**作用**: 运行Flask后端应用

#### 3. pip（Python包管理器）（已安装 ✅）
```bash
# 检查版本
pip3 --version
# 输出: pip 24.0 ✅
```
**作用**: 安装Python依赖包

### Python依赖包（通过requirements.txt安装）

这些依赖会在部署时通过 `pip install -r backend/requirements.txt` 自动安装：

```
Flask==3.0.0              # Web框架
Flask-CORS==4.0.0         # 跨域支持
Werkzeug==3.0.1           # WSGI工具库（Flask依赖）
```

**安装时机**: 在服务器上创建虚拟环境后自动安装

### 可选依赖

#### 1. firewalld（防火墙）
```bash
# 检查状态
systemctl status firewalld

# 如果未启动，启动它
systemctl start firewalld
systemctl enable firewalld
```
**作用**: 管理服务器防火墙规则，开放HTTP端口

---

## 🎯 部署方案确定

### 部署目录选择
**确定使用**: `/root/project-roadmap`
- root用户的home目录下
- 便于管理和访问
- root用户有完全权限

### 部署架构
由于服务器未安装Nginx，我们有两个选择：

#### 方案A: 安装Nginx（推荐）
- 优点: 生产环境标准配置，性能好，支持静态文件缓存
- 缺点: 需要安装配置Nginx

#### 方案B: 仅使用Flask
- 优点: 部署简单，无需额外配置
- 缺点: Flask不适合直接对外提供静态文件服务
- 需要修改Flask配置支持静态文件

**建议采用方案A**，安装Nginx作为反向代理和静态文件服务器。

---

## 🚀 部署执行计划

### 阶段1: 本地准备
```bash
# 1.1 构建前端
cd /Users/a1/Desktop/小麦/项目代码/项目路线图/frontend
npm run build
cd ..

# 1.2 创建部署包
mkdir -p ~/deploy_temp/project-roadmap
cp -r backend ~/deploy_temp/project-roadmap/
cp -r frontend/dist ~/deploy_temp/project-roadmap/frontend_dist
cp .gitignore ~/deploy_temp/project-roadmap/
cp README.md ~/deploy_temp/project-roadmap/

# 1.3 复制现有数据文件（包含已初始化的项目数据）
cp -r data ~/deploy_temp/project-roadmap/
```

### 阶段2: 上传文件
```bash
# 2.1 创建服务器目录
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 "mkdir -p /root/project-roadmap"

# 2.2 上传文件
rsync -avz --progress \
  -e "ssh -i /Users/a1/.ssh/id_rsa" \
  ~/deploy_temp/project-roadmap/ \
  root@10.0.20.178:/root/project-roadmap/
```

### 阶段3: 安装Nginx（CentOS 7）
```bash
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 << 'EOF'
# 3.1 安装Nginx
yum install -y epel-release
yum install -y nginx

# 3.2 启动Nginx
systemctl start nginx
systemctl enable nginx

# 3.3 检查状态
systemctl status nginx
nginx -v
EOF
```

### 阶段4: 配置Python环境
```bash
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 << 'EOF'
cd /root/project-roadmap

# 4.1 创建虚拟环境
python3 -m venv venv

# 4.2 激活虚拟环境并安装依赖
source venv/bin/activate
pip install -r backend/requirements.txt

# 4.3 验证安装
pip list | grep Flask
EOF
```

### 阶段5: 配置Nginx
```bash
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 << 'EOF'
# 5.1 创建Nginx配置
cat > /etc/nginx/conf.d/project-roadmap.conf << 'NGINX_CONF'
server {
    listen 80;
    server_name 10.0.20.178;

    # 前端静态文件
    location / {
        root /root/project-roadmap/frontend_dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # 后端API代理
    location /api/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        root /root/project-roadmap/frontend_dist;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
NGINX_CONF

# 5.2 测试配置
nginx -t

# 5.3 重启Nginx
systemctl restart nginx
EOF
```

### 阶段6: 配置Flask服务
```bash
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 << 'EOF'
# 6.1 创建systemd服务
cat > /etc/systemd/system/project-roadmap.service << 'SERVICE_CONF'
[Unit]
Description=Project Roadmap Flask Backend
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/project-roadmap
Environment="PATH=/root/project-roadmap/venv/bin"
ExecStart=/root/project-roadmap/venv/bin/python backend/app.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
SERVICE_CONF

# 6.2 重载systemd
systemctl daemon-reload

# 6.3 启动服务
systemctl start project-roadmap
systemctl enable project-roadmap

# 6.4 检查状态
systemctl status project-roadmap
EOF
```

### 阶段7: 配置防火墙
```bash
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 << 'EOF'
# 7.1 检查firewalld状态
systemctl status firewalld

# 7.2 开放HTTP端口
firewall-cmd --permanent --add-service=http
firewall-cmd --reload

# 7.3 查看规则
firewall-cmd --list-all
EOF
```

### 阶段8: 验证部署
```bash
# 8.1 测试API
curl http://10.0.20.178/api/productlines
curl http://10.0.20.178/api/projects
curl http://10.0.20.178/api/settings

# 8.2 检查服务状态
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 << 'EOF'
systemctl status project-roadmap
systemctl status nginx
journalctl -u project-roadmap -n 20
EOF
```

---

## 📝 部署检查清单

### 部署前
- [x] SSH连接测试通过
- [x] 服务器环境信息确认
- [x] Python版本符合要求(3.11.9)
- [x] 端口未被占用
- [ ] 本地前端构建完成
- [ ] 部署包创建完成

### 部署中
- [ ] 文件上传成功
- [ ] Nginx安装成功
- [ ] Python虚拟环境创建成功
- [ ] 依赖安装成功
- [ ] Nginx配置正确
- [ ] systemd服务配置正确
- [ ] 防火墙规则配置

### 部署后
- [ ] Flask服务运行正常
- [ ] Nginx服务运行正常
- [ ] API接口响应正常
- [ ] 浏览器可以访问
- [ ] 数据可以正常保存

---

## 🔧 快速命令参考

### 查看日志
```bash
# Flask日志
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 "journalctl -u project-roadmap -f"

# Nginx日志
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 "tail -f /var/log/nginx/error.log"
```

### 重启服务
```bash
# 重启Flask
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 "systemctl restart project-roadmap"

# 重启Nginx
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 "systemctl restart nginx"
```

### 检查状态
```bash
# 检查所有服务
ssh -i /Users/a1/.ssh/id_rsa root@10.0.20.178 "systemctl status project-roadmap nginx"
```

---

**准备就绪！可以开始执行部署流程。**

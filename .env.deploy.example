# 部署配置示例（复制为 .env.deploy 使用）
# 注意：不要将 .env.deploy 提交到 git，已在 .gitignore 中忽略。

# 云主机连接
SSH_HOST=example.com
SSH_USER=ubuntu
SSH_PORT=22

# 部署路径（云主机上）
REMOTE_DIR=/opt/project-roadmap
REMOTE_DATA_DIR=/opt/project-roadmap/data
BACKUP_DIR=/opt/project-roadmap/backup

# 可选：额外排除的相对路径（逗号分隔）
#EXCLUDES=frontend/dist,backend/__pycache__

# 可选：设置为1启用试运行（不真正执行）
#DRY_RUN=0

# 可选：启用前端构建并上传产物（默认0）
DEPLOY_FRONTEND=0
# 本地前端源码目录与构建命令
FRONTEND_DIR=frontend
FRONTEND_BUILD_CMD="npm run build"
# 远程前端产物目录（默认 ${REMOTE_DIR}/frontend_dist）
REMOTE_FRONTEND_DIR=/opt/project-roadmap/frontend_dist

# 可选：重启后端服务并进行健康检查（默认0）
RESTART_BACKEND=0
# 后端systemd服务名（默认 project-roadmap）
BACKEND_SERVICE=project-roadmap
# 健康检查URL（默认 http://127.0.0.1:5000/api/projects）
HEALTHCHECK_URL=http://127.0.0.1:5000/api/projects